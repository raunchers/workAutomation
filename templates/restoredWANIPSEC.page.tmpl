{{template "base" .}}
{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <form action="/PostRestoredWANIPSEC" method="post">
                    <div class="mb-3">
                        <textarea name="DRMInfo" id="formGroupDRM" placeholder="DRM Output" cols="70"></textarea>
                    </div>
                    <button class="btn btn-primary" type="submit">Submit</button>
                </form>
            </div>
        </div>
    </div>
{{end}}
